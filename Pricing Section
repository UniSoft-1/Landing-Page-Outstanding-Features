<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Software Development Pricing</title>
    <style>
        /* Background & Fonts */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #1f1f1f, #3a3a3a);
            color: white;
            text-align: center;
            padding: 0;
            margin: 0;
        }

        /* Container */
        .container {
            max-width: 900px;
            margin: 50px auto;
            padding: 20px;
        }

        /* Pricing Section */
        .pricing-section {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            animation: fadeIn 1s ease-in-out;
        }

        /* Section Title */
        .pricing-title {
            font-size: 32px;
            font-weight: bold;
            color: #fff;
            text-transform: uppercase;
        }

        .pricing-description {
            color: #ddd;
            font-size: 16px;
            margin: 15px 0 30px;
        }

        /* Pricing Grid */
        .pricing-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }

        /* Pricing Cards */
        .pricing-card {
            flex: 1 1 280px;
            max-width: 100%;
            background: rgba(255, 255, 255, 0.2);
            padding: 25px;
            border-radius: 12px;
            backdrop-filter: blur(15px);
            transition: transform 0.4s ease-in-out, box-shadow 0.4s ease-in-out;
            position: relative;
            overflow: hidden;
            box-shadow: 0 6px 20px rgba(255, 255, 255, 0.1);
        }

        /* Hover Effects */
        .pricing-card:hover {
            transform: scale(1.08);
            box-shadow: 0 10px 25px rgba(255, 255, 255, 0.2);
        }

        /* Card Titles */
        .pricing-card h3 {
            font-size: 22px;
            color: #fff;
            margin-bottom: 10px;
        }

        /* Price */
        .pricing-card .price {
            font-size: 18px;
            font-weight: bold;
            color: #ffcc00;
            margin-bottom: 12px;
        }

        /* Description */
        .pricing-card p {
            color: #ddd;
            font-size: 14px;
            line-height: 1.6;
        }

        /* Animated Glow */
        .pricing-card::before {
            content: "";
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.2) 0%, transparent 50%);
            transform: rotate(25deg);
            opacity: 0;
            transition: opacity 0.4s;
        }

        .pricing-card:hover::before {
            opacity: 0.3;
        }

        /* Button */
        .quote-btn {
            display: inline-block;
            margin-top: 30px;
            padding: 12px 25px;
            background: #ffcc00;
            color: #333;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            border-radius: 8px;
            transition: background 0.3s ease-in-out, transform 0.2s;
        }

        .quote-btn:hover {
            background: #ffdb4d;
            transform: scale(1.05);
        }

        /* Fade-In Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .pricing-grid {
                flex-direction: column;
                align-items: center;
            }
            .pricing-card {
                width: 90%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="pricing-section">
            <h2 class="pricing-title">Software Development Pricing</h2>
            <p class="pricing-description">
                Choose a pricing model that fits your project needs. 
                We offer flexible options for businesses of all sizes.
            </p>
            <div class="pricing-grid">
                <div class="pricing-card">
                    <h3>Hourly Rate</h3>
                    <p class="price">$50 - $150/hr</p>
                    <p>For flexible projects that require on-demand development services.</p>
                </div>
                <div class="pricing-card">
                    <h3>Fixed Price</h3>
                    <p class="price">Custom Quote</p>
                    <p>Ideal for well-defined projects with clear scope and requirements.</p>
                </div>
                <div class="pricing-card">
                    <h3>Dedicated Team</h3>
                    <p class="price">$3,000 - $15,000/month</p>
                    <p>A full-time team dedicated to your software development needs.</p>
                </div>
            </div>
            <a href="mailto:vvvsoft@victorianvanguardventures.com?subject=Quotation&body=Please%20describe%20your%20idea!" class="quote-btn">Get a Custom Quote</a>
        </div>
    </div>
</body>
</html>
