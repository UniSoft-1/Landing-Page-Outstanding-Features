<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Button with Animation</title>
    <style>
        .whatsapp-button {
            display: inline-block;
            background-color: #25D366;
            color: white;
            font-size: 16px;
            padding: 12px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            font-weight: bold;
            opacity: 0; /* Start hidden */
            transform: translateY(20px); /* Start slightly below */
            animation: fadeInUp 1s ease-out forwards; /* Apply animation */
        }

        .whatsapp-button:hover {
            background-color: #1EBE5D;
        }

        /* Keyframes for fade-in and slide-up effect */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>

    <a href="https://wa.me/5216146063201?text=Hola,%20me%20pudieras%20compartir%20más%20información%20sobre%20las%20experiencias%20en%20la%20Sierra%20Tarahumara." 
       target="_blank" class="whatsapp-button">
        Reserva via WhatsApp
    </a>

</body>
</html>
